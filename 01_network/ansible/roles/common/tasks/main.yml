---
- name: タイムゾーンを Asia/Tokyo に設定
  community.general.timezone:
    name: Asia/Tokyo

- name: パッケージリストを更新
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: パッケージを最新化
  apt:
    upgrade: dist

- name: 不要パッケージを削除
  apt:
    autoremove: true
